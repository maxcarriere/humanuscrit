# Humanuscrit API (HAPP v1)
# Le site statique est sur GitHub Pages. Netlify héberge uniquement l'API.

[build]
  command = "mkdir -p _api"
  publish = "_api"
  functions = "netlify/functions"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- netlify/ package.json package-lock.json netlify.toml openapi.yaml AGENTS.md .well-known/"

# Proxy GoatCounter — contourne les bloqueurs de pub
[[redirects]]
  from = "/gc/count.js"
  to = "https://gc.zgo.at/count.js"
  status = 200

[[redirects]]
  from = "/gc/count"
  to = "https://humanuscrit.goatcounter.com/count"
  status = 200

# Headers CORS pour l'API
[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, X-Payment, X-Agent-Id, Idempotency-Key"
